{
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "scripts": {
    "postinstall": "prisma generate --schema=backend/prisma/schema.prisma",
    "vercel-build": "npx prisma generate --schema=backend/prisma/schema.prisma && npx esbuild api/_build.ts --bundle --platform=node --format=esm --external:@prisma/client --outfile=api/index.js && cd frontend && npm install && npx vite build"
  },
  "dependencies": {
    "@prisma/client": "^7.4.0",
    "@trpc/server": "^11.10.0",
    "better-auth": "^1.4.19",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "ical.js": "^2.2.1",
    "superjson": "^2.2.6",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.2.2",
    "esbuild": "^0.25.0",
    "prisma": "^7.4.0"
  }
}
